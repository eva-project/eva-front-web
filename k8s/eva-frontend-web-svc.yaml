apiVersion: v1
kind: Service
metadata:
  labels:
    app: eva-frontend-web
  name: eva-frontend-web-svc
spec:
  ports:
  - nodePort: 32645
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: eva-frontend-web
  type: LoadBalancer
